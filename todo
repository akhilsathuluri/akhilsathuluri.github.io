1. Add pdf link directly at the project index
2. Change font size of the project title in the index
3. Cannot add summary for the blog posts
4. Add tags in projects to seperate design projects form robotics
5. Change the way links look. Make them blue in the main page atleast.

Running a local host:
Go to the host directory
bundle exec jekyll serve

Then access the page via: http://localhost:4000/
